using UnityEngine;
using System.Collections;

public class PlayerController : MonoBehaviour {

	// Use this for initialization
	void Start () {/*
        player = new Player(1, "Pierre");
        player.PlayerBoard = new System.Collections.Generic.List<Card>();
        playerCard = new Card(1, 3, Card.CardTypes.Civilian);
        player.PlayerBoard.Add(playerCard);*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
